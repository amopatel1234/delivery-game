E3-S03 hazard resolution is in (domain). Heavy Traffic delay/damage now roll via injected seeded RNG.

What to try:
1. Main Menu → Start Game and build any route (including Clear Road) — Planning summary should update without crashing.
2. Reach Destination → Confirm Route locks editing; hand-off panel still appears.
3. Execution simulation UI still comes later; domain now resolves Heavy Traffic with inclusive 1–100 rolls (damage only after delay).
4. Same seed → same hazard outcomes; event history / presentation arrive in later E3 stories.
