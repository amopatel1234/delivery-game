E4-S02 reward settlement is in (domain). Completed runs settle base/early/late/damage via shared EconomyConfiguration; failed runs always pay 0.

What to try:
1. Main Menu → Start Game → complete a route → Confirm Route; playback + recap still work.
2. Domain now settles final reward from ExecutionResult (no UI yet).
3. Outcome breakdown / results screen arrive in E4-S03 / E4-S04.
