E6-S03 accessibility support is in. VoiceOver labels/hints on key controls, Dynamic Type on chrome text, Reduce Motion skips execution step delays, contrast checks for ink/canvas, and VoiceOver announcements for route/execution/outcome.

What to try:
1. Enable VoiceOver — Start Game, grid cells, Undo/Confirm, Continue expose labels and hints.
2. Enable Larger Text — menu, planning summary, and results chrome scale; grid cells stay compact.
3. Enable Reduce Motion — Confirm Route playback advances without step delays.
4. Invalid route taps announce the rejection reason.
