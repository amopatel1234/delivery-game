E3-S02 card resolution is in (domain).

What to try:
1. Confirm a route as before — hand-off UI unchanged.
2. Execution simulation UI still comes later; card travel/delay rules now resolve in the domain engine.
3. Heavy Traffic hazard rolls arrive in E3-S03.
