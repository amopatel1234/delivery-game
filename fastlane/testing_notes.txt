E4-S03 outcome breakdown is in (domain). Settlement maps to an immutable itemised breakdown (base, early, late, damage, final + timing summary).

What to try:
1. Main Menu → Start Game → complete a route → Confirm Route; playback + recap still work.
2. Domain now exposes OutcomeBreakdown from ExecutionResult (no UI yet).
3. Results screen with breakdown presentation arrives in E4-S04.
