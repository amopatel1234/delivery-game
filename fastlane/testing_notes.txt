E3-S07 execution completion is in (domain). After a run finishes, ExecutionEngine.finish() emits an immutable ExecutionResult once for Epic 4 settlement.

What to try:
1. Main Menu → Start Game → complete a route → Confirm Route and watch playback + recap.
2. Domain hand-off now includes sealed event history, elapsed time, damage, recap, and destination flag.
3. Outcome / reward UI still comes in Epic 4 (E4-S01 next).
